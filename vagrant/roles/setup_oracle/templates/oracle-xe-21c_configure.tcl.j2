#!/usr/bin/expect -f
set env(ORAPASS) {{ oraPass }}

set timeout 3600
log_file ~/oracle-xe-21c_configure.log
log_user 1

spawn /etc/init.d/oracle-xe-21c configure

expect "Specify a password*"
send "$env(ORAPASS)\r";

expect "Confirm the password:"
send "$env(ORAPASS)\r";


expect eof
